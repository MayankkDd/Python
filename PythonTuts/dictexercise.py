word_meaning_dict = {"mutable":"which can be changed", "immutable":"which can not be changed",
                     "apple":"a kind of fruit red in color", "pen":"an object by which we can write on the paper"}
user_input=input();
print(word_meaning_dict[user_input])